#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int solve()
{
    int n;
    cin >> n;

    if(n == 1)
        return 0;

    vector<int> v(n);
    
    for(int i = 0; i < n; i++)
        cin >> v[i];
    

        
}

int main()
{
    ios::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    int N;
    cin >> N;

    for (int i = 0; i < N; i++)
        cout << solve() << endl;
    return 0;
}