#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int solve()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    int N;
    cin >> N;

    for (int i = 0; i < N; i++)
        solve();
    return 0;
}