#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int32_t main()
{
    ios::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    int T;
    cin >> T;
    vector<int> claws(T);
    bitset<1000000> bt(T);

    return 0;
}
