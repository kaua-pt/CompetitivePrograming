#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using pp = pair<int, int>;

int solve()
{
    int n;
    cin >> n;
}

int32_t main()
{
    ios::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    int T;
    cin >> T;

    while (T--)
        cout << solve() << endl;
    return 0;
}
