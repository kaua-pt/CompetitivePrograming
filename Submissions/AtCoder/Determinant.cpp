#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int32_t main()
{
    ios::sync_with_stdio(false);
    cout.tie(0);
    cin.tie(0);

    int a, b, c, d;
    cin >> a >> b;
    cin >> c >> d;
    cout << (a * d) - (c * b) << endl;

    return 0;
}
