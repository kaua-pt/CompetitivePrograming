#include <bits/stdc++.h>

using namespace std;

int main()
{
    int num, ok;
    cin >> num >> ok;
    vector<int> pesos(num);

    for (int i = 0; i < num; i++)
    {
        cin >> pesos[i];
    }

    for (int i = 0; i < num - 1; i++)
    {
        int a, b;
        cin >> a >> b;
        }

    return 0;
}